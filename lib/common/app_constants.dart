class AppConstants {
  static const String appName = 'Dayfi';

  /// Standard horizontal inset for feature screens (lists, cards).
  static const double screenHorizontalPadding = 18;

  /// Extra inset applied to full-width primary/secondary buttons inside screens.
  static const double buttonExtraHorizontalPadding = 24;
}
